<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>401 - Unauthorized</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gradient-to-br from-gray-900 to-black flex items-center justify-center h-screen text-center"
  >
    <div
      class="w-full max-w-lg bg-gray-800/50 rounded-lg shadow-xl p-12 backdrop-blur-sm"
    >
      <h1
        class="text-8xl font-bold mb-4 text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-orange-500"
      >
        401
      </h1>
      <p class="text-2xl font-bold text-white mb-4">Unauthorized Access</p>
      <p class="text-lg text-gray-300 mb-8">
        Sorry, you do not have permission to access this page. Please log in to
        continue.
      </p>
      <a
        href="/login"
        class="bg-gradient-to-r from-blue-600 to-teal-600 hover:from-blue-700 hover:to-teal-700 text-white font-bold py-3 px-6 rounded-full focus:outline-none focus:shadow-outline transition duration-300"
      >
        Go to Login
      </a>
    </div>
    <script>
      (async () => {
        try {
          const res = await fetch('/refresh', { method: 'post' });

          if (res.ok) {
            window.location.reload();
          }
          else {
            window.location.href = '/login?error=Session Expired';
          }
        }
        catch (err) {
          window.location.href = '/oops';
        }
      })();
    </script>
  </body>
</html>
