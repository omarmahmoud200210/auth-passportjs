<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gradient-to-br from-gray-900 to-black flex items-center justify-center h-screen text-center"
  >
    <div
      class="w-full max-w-4xl bg-gray-800/50 rounded-lg shadow-xl p-12 backdrop-blur-sm"
    >
      <h1 class="text-5xl font-bold mb-4 text-white">
        Welcome,
        <span
          class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-teal-400"
          ><%= user %></span
        >!
      </h1>
      <% if (role === 'admin') { %>
        <p class="text-xl text-green-300 mb-2"><%= user %>, You're the admin</p>
      <% } %>
      <p class="text-xl text-gray-300 mb-8">
        You have successfully logged in. This is your personal dashboard.
      </p>
      <a 
        href="/logout"
        class="bg-gradient-to-r from-red-600 to-pink-600 hover:from-red-700 hover:to-pink-700 text-white font-bold py-3 px-6 rounded-full focus:outline-none focus:shadow-outline transition duration-300"
      >
        Log Out
      </a>
    </div>
  </body>
</html>
